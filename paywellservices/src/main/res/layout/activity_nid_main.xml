<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/relativeLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".activity.utility.nid.NidMainActivity">

    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="5dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginBottom="@dimen/activity_horizontal_margin"
                android:orientation="horizontal">

                <Button
                    android:id="@+id/homeBtnCorrection"
                    style="@style/HomeButton"
                    android:layout_marginTop="15dp"
                    android:drawableTop="@drawable/ic_home_nid"
                    android:onClick="onButtonClicker"
                    android:paddingBottom="5dp"
                    android:text="@string/home_utility_nid_correction"
                    android:textAllCaps="false"
                    android:textAppearance="?android:attr/textAppearanceMedium" />

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:layout_weight="1"
                    android:gravity="center"></LinearLayout>

                <Button
                    android:id="@+id/homeBtnInquiry"
                    style="@style/HomeButton"
                    android:layout_marginTop="15dp"
                    android:drawableTop="@drawable/ic_home_pb_inquiry_billpay"
                    android:onClick="onButtonClicker"
                    android:paddingBottom="5dp"
                    android:text="@string/home_utility_passport_status_inquiry"
                    android:textAllCaps="false"
                    android:textAppearance="?android:attr/textAppearanceMedium" />

            </LinearLayout>
        </LinearLayout>
    </ScrollView>

</RelativeLayout>
